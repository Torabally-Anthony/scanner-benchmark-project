apiVersion: v1
kind: Service
metadata:
  name: public-helm-service
spec:
  type: {{ .Values.service.type }}
  selector:
    app: demo-app
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080