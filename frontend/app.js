"use strict";

const scanners = ["checkov", "trivy", "kubescape"];
const titles = {dashboard:"Dashboard",corpus:"Benchmark Corpus",run:"Run Benchmark",raw:"Raw Scanner Outputs",normalised:"Normalised Findings",matched:"Matching Results",metrics:"Precision / Recall / F1",reports:"Reports",settings:"Settings"};
const state = {view:"dashboard",metrics:new Map(),latest:null};
const $ = (id) => document.getElementById(id);

function apiBase(){const saved=localStorage.getItem("scannerBenchmarkApiBase");if(saved!==null)return saved.replace(/\/+$/,"");return location.port==="8000"?"":"http://127.0.0.1:8000"}
function url(path){return `${apiBase()}${path}`}
function esc(value){return String(value??"").replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;")}
function scannerName(name){return name.charAt(0).toUpperCase()+name.slice(1)}
function metric(value){const n=Number(value);return Number.isFinite(n)?n.toFixed(2):"—"}
function dateText(value){const d=new Date(value);return value&&!Number.isNaN(d.valueOf())?d.toLocaleString():"—"}
function toast(message,type="info"){const box=$("toast");box.textContent=message;box.className=`toast ${type}`;box.hidden=false;clearTimeout(toast.timer);toast.timer=setTimeout(()=>box.hidden=true,4200)}

async function request(path,options={}){const controller=new AbortController();const timer=setTimeout(()=>controller.abort(),320000);try{const response=await fetch(url(path),{...options,headers:{"Content-Type":"application/json",...(options.headers||{})},signal:controller.signal});const kind=response.headers.get("content-type")||"";const data=kind.includes("application/json")?await response.json():await response.text();if(!response.ok)throw new Error(typeof data==="object"?(data.detail||data.message||JSON.stringify(data)):data||`HTTP ${response.status}`);return data}catch(error){if(error.name==="AbortError")throw new Error("The request timed out.");throw error}finally{clearTimeout(timer)}}

function connection(online,text){$("connection").className=`connection ${online?"online":"offline"}`;$("connection").querySelector("b").textContent=online?"Backend connected":"Backend offline";$("sideDot").className=online?"online":"offline";$("sideText").textContent=text}
async function health(show=false){try{const data=await request("/api/health");connection(true,`${data.project||"Backend"} online`);if(show)toast("Backend connection successful.","success");return true}catch(error){connection(false,error.message);if(show)toast("Backend unavailable. Start Uvicorn on port 8000.","error");return false}}

function showView(name){state.view=name;document.querySelectorAll(".nav").forEach(b=>b.classList.toggle("active",b.dataset.view===name));document.querySelectorAll(".view").forEach(v=>v.classList.remove("active"));let id=name;if(["raw","normalised","matched"].includes(name)){id="output";setOutputStage(name)}$(id).classList.add("active");$("pageTitle").textContent=titles[name]||"Scanner Benchmark";scrollTo({top:0,behavior:"smooth"})}
function setOutputStage(stage){$("outputStage").value=stage;const copy={raw:["Raw scanner outputs","Inspect the original machine-readable scanner output."],normalised:["Normalised findings","Review findings converted into the shared benchmark schema."],matched:["Matching results","Review ground-truth classifications and evidence."],metrics:["Metric output","Inspect the generated metric JSON file."]};const item=copy[stage]||copy.raw;$("outputTitle").textContent=item[0];$("outputDesc").textContent=item[1]}

function fillCases(cases){const source=cases.length?cases:[{case_id:"case-001-privileged-container"}];["dashboardCase","caseSelect","outputCase","metricsCase"].forEach(id=>{const select=$(id),current=select.value;select.innerHTML=source.map(c=>`<option value="${esc(c.case_id)}">${esc(c.case_id)}</option>`).join("");if([...select.options].some(o=>o.value===current))select.value=current})}
async function loadCases(){try{const data=await request("/api/cases");const cases=data.cases||[];fillCases(cases);$("corpusTable").innerHTML=cases.length?cases.map(c=>`<tr><td><strong>${esc(c.case_id)}</strong></td><td>${esc(c.artifact_type||"unknown")}</td><td>${esc(c.artifact_name||"Missing")}</td><td>${esc(c.ground_truth_name||"Missing")}</td><td>${esc(c.severity||"—")}</td><td><span class="pill ${c.validation_status==="valid"?"success":"neutral"}">${esc(c.validation_status||"unknown")}</span></td></tr>`).join(""):'<tr><td colspan="6" class="empty">No benchmark cases found.</td></tr>'}catch(error){$("corpusTable").innerHTML=`<tr><td colspan="6" class="empty">Could not load corpus: ${esc(error.message)}</td></tr>`}}

function standardMetrics(scanner,response){const m=response.metrics||response.data?.metrics||response.data||response;const counts=response.counts||response.data?.counts||m.counts||{};const values=m.metrics||m;return{scanner,case_id:response.case_id||m.case_id||"unknown",status:"available",tp:Number(counts.true_positive_count??counts.tp??m.true_positive_count??0),fp:Number(counts.false_positive_count??counts.fp??m.false_positive_count??0),fn:Number(counts.false_negative_count??counts.fn??m.false_negative_count??0),extras:Number(counts.unlabelled_extra_findings_count??counts.unlabelled_extra_count??m.unlabelled_extra_findings_count??0),precision:Number(values.precision),recall:Number(values.recall),f1:Number(values.f1_score??values.f1),updated_at:response.updated_at||null}}
async function oneMetric(scanner,caseId){try{return standardMetrics(scanner,await request(`/api/metrics/${scanner}?case_id=${encodeURIComponent(caseId)}`))}catch(error){return{scanner,case_id:caseId,status:"missing",error:error.message,tp:0,fp:0,fn:0,extras:0,precision:NaN,recall:NaN,f1:NaN,updated_at:null}}}
async function loadMetrics(caseId=$("metricsCase").value){const rows=await Promise.all(scanners.map(s=>oneMetric(s,caseId)));rows.forEach(r=>state.metrics.set(`${caseId}:${r.scanner}`,r));renderMetrics(rows);const first=rows.find(r=>r.status==="available");first?selectMetric(first):clearMetrics();return rows}
function renderMetrics(rows){$("dashboardTable").innerHTML=rows.map(r=>`<tr class="clickable" data-scanner="${r.scanner}"><td><strong>${scannerName(r.scanner)}</strong></td><td><span class="pill ${r.status==="available"?"success":"neutral"}">${r.status==="available"?"Available":"Not generated"}</span></td><td>${r.tp}</td><td>${r.fp}</td><td>${r.fn}</td><td>${r.extras}</td><td>${metric(r.precision)}</td><td>${metric(r.recall)}</td><td>${metric(r.f1)}</td></tr>`).join("");$("metricsTable").innerHTML=rows.map(r=>`<tr class="clickable" data-scanner="${r.scanner}"><td><strong>${scannerName(r.scanner)}</strong></td><td>${r.tp}</td><td>${r.fp}</td><td>${r.fn}</td><td>${r.extras}</td><td>${metric(r.precision)}</td><td>${metric(r.recall)}</td><td>${metric(r.f1)}</td></tr>`).join("");document.querySelectorAll("tr.clickable").forEach(row=>row.onclick=()=>{const item=state.metrics.get(`${$("metricsCase").value}:${row.dataset.scanner}`)||state.metrics.get(`${$("dashboardCase").value}:${row.dataset.scanner}`);if(item)selectMetric(item)})}
function clearMetrics(){["dashPrecision","dashRecall","dashF1","metricPrecision","metricRecall","metricF1"].forEach(id=>$(id).textContent="—");$("precisionCounts").textContent="TP — · FP —";$("recallCounts").textContent="TP — · FN —"}
function selectMetric(item){$("dashPrecision").textContent=metric(item.precision);$("dashRecall").textContent=metric(item.recall);$("dashF1").textContent=metric(item.f1);$("metricPrecision").textContent=metric(item.precision);$("metricRecall").textContent=metric(item.recall);$("metricF1").textContent=metric(item.f1);$("precisionCounts").textContent=`TP ${item.tp} · FP ${item.fp}`;$("recallCounts").textContent=`TP ${item.tp} · FN ${item.fn}`;setLatest({case_id:item.case_id,scanner:item.scanner,matching_mode:state.latest?.matching_mode||"review",status:item.status==="available"?"completed":"missing",updated_at:item.updated_at||new Date().toISOString()})}
function setLatest(x){state.latest=x;$("latestCase").textContent=x.case_id||"—";$("latestScanner").textContent=x.scanner?scannerName(x.scanner):"—";$("latestMode").textContent=(x.matching_mode||"—").replace(/^./,c=>c.toUpperCase());$("latestUpdated").textContent=dateText(x.updated_at);$("latestStatus").className=`pill ${x.status==="completed"?"success":"neutral"}`;$("latestStatus").textContent=x.status||"Unknown"}

function log(message){const stamp=new Date().toLocaleTimeString(),box=$("console");box.textContent=box.textContent==="Ready to run a benchmark."?`[${stamp}] ${message}`:`${box.textContent}\n[${stamp}] ${message}`;box.scrollTop=box.scrollHeight}
async function runBenchmark(event){event.preventDefault();const selected=[...document.querySelectorAll('input[name="scanner"]:checked')].map(x=>x.value);if(!selected.length)return toast("Select at least one scanner.","error");const caseId=$("caseSelect").value,mode=$("modeSelect").value;$("runBtn").disabled=true;$("runBtn").textContent="Running benchmark…";$("progressBar").style.width="12%";log(`Starting ${selected.join(", ")} for ${caseId}.`);try{const response=await request("/api/run",{method:"POST",body:JSON.stringify({case_id:caseId,scanners:selected,matching_mode:mode})});$("progressBar").style.width="100%";(response.results||[]).forEach(r=>{log(`${scannerName(r.scanner)}: ${r.status}.`);if(r.console_output)log(r.console_output.trim());if(r.error)log(`Error: ${r.error}`)});const completed=(response.results||[]).filter(r=>r.status==="completed");if(completed.length){setLatest({case_id:caseId,scanner:completed.at(-1).scanner,matching_mode:mode,status:"completed",updated_at:new Date().toISOString()});toast(`${completed.length} scanner pipeline(s) completed.`,"success")}else toast("No scanner pipeline completed. Check the console.","error");await loadMetrics(caseId);await loadReports()}catch(error){log(`Benchmark failed: ${error.message}`);toast(`Benchmark failed: ${error.message}`,"error")}finally{$("runBtn").disabled=false;$("runBtn").textContent="Run selected scanners";setTimeout(()=>$("progressBar").style.width="0",900)}}
async function loadSaved(){const rows=await loadMetrics($("caseSelect").value);const count=rows.filter(r=>r.status==="available").length;if(count){toast(`${count} saved metric file(s) loaded.`,"success");showView("metrics")}else toast("No saved metric files found.","error")}
async function loadOutput(){const stage=$("outputStage").value,scanner=$("outputScanner").value,caseId=$("outputCase").value;$("outputViewer").textContent="Loading output…";try{const data=await request(`/api/outputs/${stage}/${scanner}?case_id=${encodeURIComponent(caseId)}`);$("outputViewer").textContent=JSON.stringify(data.data??data,null,2)}catch(error){$("outputViewer").textContent=`Could not load output.\n\n${error.message}`}}
async function loadReports(){try{const data=await request("/api/reports"),reports=data.reports||[];$("reportList").innerHTML=reports.length?reports.map(r=>`<button class="report-item" data-name="${esc(r.name)}"><strong>${esc(r.name)}</strong><small>${dateText(r.modified_at)}</small></button>`).join(""):'<p class="empty">No generated reports found.</p>';document.querySelectorAll(".report-item").forEach(b=>b.onclick=()=>openReport(b.dataset.name,b))}catch(error){$("reportList").innerHTML=`<p class="empty">Could not load reports: ${esc(error.message)}</p>`}}
async function openReport(name,button){document.querySelectorAll(".report-item").forEach(x=>x.classList.remove("active"));button.classList.add("active");$("reportTitle").textContent=name;$("reportViewer").textContent="Loading report…";try{const response=await fetch(url(`/api/reports/${encodeURIComponent(name)}`));if(!response.ok)throw new Error(`HTTP ${response.status}`);$("reportViewer").textContent=await response.text()}catch(error){$("reportViewer").textContent=`Could not load report.\n\n${error.message}`}}

async function refresh(){if(!await health())return toast("Backend unavailable. Start FastAPI first.","error");if(state.view==="corpus")await loadCases();else if(state.view==="reports")await loadReports();else if(state.view==="metrics"||state.view==="dashboard")await loadMetrics(state.view==="dashboard"?$("dashboardCase").value:$("metricsCase").value);else if(["raw","normalised","matched"].includes(state.view))await loadOutput();else await Promise.all([loadCases(),loadMetrics(),loadReports()]);toast("Data refreshed.","success")}
function bind(){document.querySelectorAll(".nav").forEach(b=>b.onclick=()=>showView(b.dataset.view));document.querySelectorAll(".goto").forEach(b=>b.onclick=()=>showView(b.dataset.target));$("refreshBtn").onclick=refresh;$("reloadCases").onclick=loadCases;$("runForm").onsubmit=runBenchmark;$("loadSavedBtn").onclick=loadSaved;$("clearConsole").onclick=()=>$("console").textContent="Ready to run a benchmark.";$("loadOutputBtn").onclick=loadOutput;$("outputStage").onchange=()=>setOutputStage($("outputStage").value);$("reloadMetrics").onclick=()=>loadMetrics($("metricsCase").value);$("metricsCase").onchange=()=>loadMetrics($("metricsCase").value);$("dashboardCase").onchange=()=>loadMetrics($("dashboardCase").value);$("reloadReports").onclick=loadReports;$("saveApi").onclick=()=>{localStorage.setItem("scannerBenchmarkApiBase",$("apiInput").value.trim().replace(/\/+$/,""));toast("API setting saved.","success");health(true)};$("testApi").onclick=()=>health(true)}
async function init(){bind();$("apiInput").value=apiBase()||location.origin;if(!await health()){toast("Frontend loaded, but the backend is not reachable. Start Uvicorn on port 8000.","error");return}await loadCases();await Promise.all([loadMetrics($("dashboardCase").value),loadReports()])}
document.addEventListener("DOMContentLoaded",init);
